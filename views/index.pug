extends layout

block content
  h1= title
  p Welcome to #{title}

  //each message in messages 
  //  p #{message.title}
  //  p #{message.message}
  //  p #{message.timestamp}
  //  p #{message.user}